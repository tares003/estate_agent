# pnpm configuration
# Strict peer deps surface real version mismatches early.
strict-peer-dependencies=false
# Keep the lockfile authoritative in CI.
prefer-frozen-lockfile=true
# Hoist nothing implicitly — workspace packages declare their own deps.
shamefully-hoist=false
