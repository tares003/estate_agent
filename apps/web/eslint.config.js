// App code is product code — the @estate guards (audit-log G4, gdpr-consent G5,
// naming G6, design-token G7, trust-marker G8, pack-entitlement G12) apply here
// via the shared React config. Build artefacts are ignored by base.js.
export { default } from '@estate/config/eslint/react';
