{
  "name": "@estate/tokens",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Design tokens ported verbatim from design/canvas/tokens.css — CSS custom properties + a type-safe TS accessor. The single source of raw visual values (G7-exempt).",
  "license": "UNLICENSED",
  "exports": {
    ".": "./src/index.ts",
    "./tokens.css": "./src/tokens.css",
    "./base.css": "./src/base.css"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@estate/config": "workspace:*",
    "@vitest/coverage-v8": "^3.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
