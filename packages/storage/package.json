{
  "name": "@estate/storage",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "StorageBackend interface + local-filesystem default implementation (CLAUDE.md §9). Files served through a signed-URL route handler; swappable to S3-compatible later without touching feature code.",
  "license": "UNLICENSED",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@estate/config": "workspace:*",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
