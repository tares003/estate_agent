{
  "name": "@estate/observability",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Structured logging (pino, JSON to stdout) + an ErrorReporter interface (no-op default; Sentry/GlitchTip swappable later). CLAUDE.md §9.",
  "license": "UNLICENSED",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "pino": "^9.0.0"
  },
  "devDependencies": {
    "@estate/config": "workspace:*",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
