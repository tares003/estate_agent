{
  "name": "@estate/email",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Per-tenant SMTP send abstraction (nodemailer) + AES-256-GCM credential resolver — the platform never holds tenant SMTP credentials in plaintext (CLAUDE.md §9). React Email templates are a follow-on.",
  "license": "UNLICENSED",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "nodemailer": "^6.9.0"
  },
  "devDependencies": {
    "@estate/config": "workspace:*",
    "@types/node": "^22.0.0",
    "@types/nodemailer": "^6.4.0",
    "@vitest/coverage-v8": "^3.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
