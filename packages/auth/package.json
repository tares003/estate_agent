{
  "name": "@estate/auth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Better Auth configuration + RBAC roles and access helpers (EPIC-N). OAuth (Microsoft/Google/Apple) + magic-link + WebAuthn; the session cookie carries the tenant identifier.",
  "license": "UNLICENSED",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@estate/db": "workspace:*",
    "better-auth": "^1.0.0"
  },
  "devDependencies": {
    "@estate/config": "workspace:*",
    "@vitest/coverage-v8": "^3.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
