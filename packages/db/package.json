{
  "name": "@estate/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Prisma schema, generated client, RLS + PostGIS raw-SQL migrations, the per-request tenant GUC extension, and the Prisma-backed PackSource. Shared-DB + Row-Level-Security multi-tenancy (EPIC-S/§J).",
  "license": "UNLICENSED",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "prisma",
    "migrations"
  ],
  "scripts": {
    "db:generate": "prisma generate",
    "db:validate": "prisma validate",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@estate/entitlement": "workspace:*",
    "@prisma/client": "^6.1.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.2.0",
    "@estate/config": "workspace:*",
    "@testcontainers/postgresql": "^10.13.0",
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.0",
    "@vitest/coverage-v8": "^3.0.0",
    "eslint": "^9.0.0",
    "pg": "^8.13.0",
    "prisma": "^6.1.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
