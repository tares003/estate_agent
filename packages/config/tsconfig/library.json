{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Estate Platform — TypeScript library package (typecheck, no emit)",
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true
  },
  "exclude": ["node_modules", "dist", ".turbo", "coverage"]
}
