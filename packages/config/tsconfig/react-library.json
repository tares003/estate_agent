{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Estate Platform — React component library (jsx, typecheck, no emit)",
  "extends": "./library.json",
  "compilerOptions": {
    "jsx": "react-jsx",
    "lib": ["ES2023", "DOM", "DOM.Iterable"]
  }
}
