# Design canvas location
#
# Format: one URL or one local path per line. Lines starting with '#' are ignored.

./design/canvas/

# Status: SET (local). The canvas is the visual source of truth for all component
# and screen work — built against, tested against (visual regression mirrors
# design/canvas/responsive-coverage.json), and not drifted from without a logged
# DESIGN.md amendment + a paired canvas update.
#
# History: originally committed at design/design/canvas/ (accidental double-nesting);
# flattened to design/canvas/ in Phase B0 to match all brief + build-prompt references.
# See audit/audit-report.md row D-004.
