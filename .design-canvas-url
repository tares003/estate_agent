# Design canvas location
#
# Format: one URL or one local path per line. Lines starting with '#' are ignored.
# When the design canvas is created, record its location below.
#
# Until then, this file exists so the autonomous build prompt can discover it
# and report "no design canvas set" rather than failing on a missing file.

status: not-yet-created

# When set, expected formats include:
#   https://www.figma.com/file/...
#   https://www.framer.com/projects/...
#   ./design/canvas/                       (local path inside this repo)
#
# Notes for the autonomous agent (per the build prompt's STEP 0c):
# - If this file says "status: not-yet-created", ASK the user once for the URL
#   or local path before doing visual work.
# - Cache the canvas under ./design/canvas/ regardless of original location.
